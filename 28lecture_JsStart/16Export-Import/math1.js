// Some JavaScript code that performs mathematical operations:

// For Default Export:
// In default export, we can export only one function/variable/class from a file.


// We need to add "export default" before the function/variable/class to make it a default export.

export default function add(a, b) {
    console.log('Adding numbers:', a+b);
}


